#pragma once

#include "Matrix.cuh"
#include "Kernel.cuh"

//void Conv2dCPU(const Matrix& in, const Kernel2d& k, Matrix& out);
//void Conv2dGPU(Matrix& in, Kernel2d& k, Matrix& out);
//void Conv2dGPUWithSharedMemory(Matrix& in, Kernel2d& k, Matrix& out);